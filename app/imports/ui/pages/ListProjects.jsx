import React from 'react';
import ListProjectsWidget from '../components/ListProjectsWidget';

class ListProjects extends React.Component {
  render() {
    return (
      <ListProjectsWidget/>
    );
  }
}

export default (ListProjects);
